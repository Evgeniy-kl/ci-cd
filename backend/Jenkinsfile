stage('test') {
     agent {
          docker {
               image 'python'
          }
     }
     steps {
          ["sh", "-c", "pip install pipenv && pipenv install --deploy && pytest"].execute()
     }
}
