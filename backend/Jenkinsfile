stage('test') {
     agent {
          docker {
               image 'python'
          }
     }
     steps {
          sh 'pip insta pipenv&& pipenv install --deploy && pipenv shell && pytest'
     }
}
