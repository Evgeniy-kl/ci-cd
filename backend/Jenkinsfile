
pipeline {
    agent { docker { image 'python:3.10.1-alpine' } }
    stages {
        stage('testing') {
            steps {
                sh 'pipenv shell && pipenv install && pipenv lock'
                sh 'pytest'
            }
        }
    }
}