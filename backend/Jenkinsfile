stage('test') {
     agent {
          docker {
               image 'python'
          }
     }
     steps {
          sh '
           pip install pipenv &&
           pipenv shell &&
           pipenv install --deploy &&
           pytest'
     }
}
