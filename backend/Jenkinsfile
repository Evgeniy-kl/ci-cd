stage('test') {
     agent {
          docker {
               image 'python'
          }
     }
     steps {
          sh 'pip insta pipenv && pipenv shell && pipenv install --deploy && pytest'
     }
}
